import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Note: Do NOT use "standalone" when deploying to Vercel.
  // Vercel has its own optimized build pipeline.
  // Only enable standalone for Docker deployments:
  // output: "standalone",
};

export default nextConfig;
